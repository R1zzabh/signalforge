# Scenario catalog

Scenario execution is implemented by `lab/controller/app.py` and mirrored in the
`/lab` control center: normal login, brute force, credential stuffing, port scan,
C2 simulation, CVE lab simulation, privileged account, allowlisted scanner, and
alert burst.
