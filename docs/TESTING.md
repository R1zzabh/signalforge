# Testing

Run `pytest -q backend/tests` after installing backend requirements. The tests cover critical brute-force scoring, port-scan mapping, and benign scoring. Manual smoke testing is documented in `FINAL_STATUS.md`.
