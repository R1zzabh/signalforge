# Known limitations

The default offline mode uses deterministic fixtures. Live provider HTTP adapters, full Outlook delivery, authentication, and enterprise queueing are extension points rather than enabled production integrations. The included UiPath XAML is an inspectable orchestration scaffold and may need activity namespace adjustments for a specific Studio version.
