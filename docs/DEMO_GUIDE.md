# Demo guide

The strongest sequence is `/demo` → SSH Brute Force → Run Scenario → open incident → show rules, MITRE T1110, timeline, then `/audit` and `/analytics`. Port Scan demonstrates a different medium-risk branch and T1046. Benign Login demonstrates the low-risk path.
