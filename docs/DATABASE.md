# Database

SQLite tables: `alerts`, `incidents`, `audit_logs`, `notifications`, and `system_runs`. Complete investigation JSON is stored so the UI can demonstrate the whole object without reconstructing state from disconnected mock records.
